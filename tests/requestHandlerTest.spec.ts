import { test, expect } from '@playwright/test';
import { RequestHandler } from '../utils/request-handler';

test('How to use the ResquestHandler methods', async ({}) => {

    // Create a new ResquestHandler instance to get access to the methods
    // const api = new RequestHandler()

    // api
    //     .url('https://conduit-api.bondaracademy.com/api')
    //     .path('/articles')
    //     .queryParams({limit:10, offset:0})
    //     .headers({Authorization: 'token'})
    //     .body({"user":{"email":"bruno@teste.pt","password":"12345678","username":"BrunoTeste"}})

})
